<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>
  <!-- Define LINUX constant for Linux platforms -->
  <PropertyGroup Condition="'$(RuntimeIdentifier)' == 'linux-x64' OR '$(RuntimeIdentifier)' == 'linux-arm64'">
    <DefineConstants>$(DefineConstants);LINUX</DefineConstants>
  </PropertyGroup>
  <!-- Define WINDOWS constant for Windows platforms -->
  <PropertyGroup Condition="'$(RuntimeIdentifier)' == 'win-x64' OR '$(RuntimeIdentifier)' == 'win-arm64' OR '$(RuntimeIdentifier)' == 'win-x86'">
    <DefineConstants>$(DefineConstants);WINDOWS</DefineConstants>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.6.0" />
    <PackageReference Include="xunit" Version="2.4.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.4.5">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="coverlet.collector" Version="6.0.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="SharpZipLib" Version="1.4.2" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="../UtilitiesLibrary/UtilitiesLibrary.csproj" />
    <ProjectReference Include="../DeDuBa/DeDuBa.csproj" />
  </ItemGroup>
  <!-- Conditional platform-specific references -->
  <ItemGroup Condition="'$(RuntimeIdentifier)' == 'linux-x64' OR '$(RuntimeIdentifier)' == '' OR '$(RuntimeIdentifier)' == 'linux-arm64'">
    <ProjectReference Include="../OsCallsLinux/OsCallsLinux.csproj" />
  </ItemGroup>
  <ItemGroup Condition="'$(RuntimeIdentifier)' == 'win-x64' OR '$(RuntimeIdentifier)' == 'win-arm64' OR '$(RuntimeIdentifier)' == 'win-x86'">
    <ProjectReference Include="../OsCallsWindows/OsCallsWindows.csproj" />
  </ItemGroup>
  <!-- Copy native libraries to test output for Linux -->
  <Target
    Name="CopyNativeLibsForTests"
    AfterTargets="Build"
    Condition="'$(RuntimeIdentifier)' == 'linux-x64' OR '$(RuntimeIdentifier)' == 'linux-arm64' OR '$(RuntimeIdentifier)' == ''"
  >
    <Copy
      SourceFiles="../OsCallsCommonShim/bin/$(Configuration)/net8.0/libOsCallsCommonShim.so"
      DestinationFolder="$(OutputPath)"
      SkipUnchangedFiles="true"
    />
    <Copy
      SourceFiles="../OsCallsLinuxShim/bin/$(Configuration)/net8.0/libOsCallsLinuxShim.so"
      DestinationFolder="$(OutputPath)"
      SkipUnchangedFiles="true"
    />
  </Target>
  <!-- Copy native libraries to test output for Windows -->
  <Target
    Name="CopyNativeLibsForTestsWindows"
    AfterTargets="Build"
    Condition="'$(RuntimeIdentifier)' == 'win-x64' OR '$(RuntimeIdentifier)' == 'win-arm64' OR '$(RuntimeIdentifier)' == 'win-x86'"
  >
    <Copy
      SourceFiles="../OsCallsCommonShim/bin/$(Configuration)/net8.0/$(Configuration)/OsCallsCommonShim.dll"
      DestinationFolder="$(OutputPath)"
      SkipUnchangedFiles="true"
    />
    <Copy
      SourceFiles="../OsCallsWindowsShim/build-win-x64/bin/$(Configuration)/OsCallsWindowsShimNative.dll"
      DestinationFolder="$(OutputPath)"
      SkipUnchangedFiles="true"
      Condition="Exists('../OsCallsWindowsShim/build-win-x64/bin/$(Configuration)/OsCallsWindowsShimNative.dll')"
    />
  </Target>
</Project>
